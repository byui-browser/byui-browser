interface:
  display_name: "fix-CI"
  short_description: "Find failing PR checks, inspect logs or external check links, and apply focused fixes"
  default_prompt: "Use $fix-CI to fix the PR's CI in GitHub actions on the current branch or the branch/pr provided."

policy:
  allow_implicit_invocation: true